import { ConfirmpassssDirective } from './confirmpassss.directive';

describe('ConfirmpassssDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmpassssDirective();
    expect(directive).toBeTruthy();
  });
});
