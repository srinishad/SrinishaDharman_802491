import { ConfirmpasssDirective } from './confirmpasss.directive';

describe('ConfirmpasssDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmpasssDirective();
    expect(directive).toBeTruthy();
  });
});
